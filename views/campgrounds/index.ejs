<% layout('layouts/boilerplate') %>

    <h1>All Campgrounds</h1>
    <a href="campgrounds/new">Add New Camp ⛺</a>
    <ul>
        <% for (let camp in campgrounds){ %>
            <li>
                <%= campgrounds[camp].title %>: <%= campgrounds[camp].city %> <a
                            href="/campgrounds/<%= campgrounds[camp]._id %>">Check out 👀</a>
            </li>
            <% } %>
    </ul>